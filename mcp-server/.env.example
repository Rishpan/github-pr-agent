# GitHub (optional for public repos; improves rate limits)
# Optional; improves GitHub API rate limits for public repos
GITHUB_TOKEN=

# RAG / semantic_search / index_repo (same as rag/.env.example)
CHROMA_HOST=localhost
CHROMA_PORT=8000
OLLAMA_HOST=localhost
OLLAMA_PORT=11434
OLLAMA_EMBED_MODEL=nomic-embed-text

# Optional semantic search tuning (same vars as rag/.env.example)
# RAG_MIN_SIMILARITY=0.45
# RAG_RELATIVE_CUTOFF_DELTA=0.08
# RAG_OVER_FETCH_MULTIPLIER=3
